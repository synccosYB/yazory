(()=>{'use strict';const form=document.querySelector('#donation-options');if(!form)return;const error=document.querySelector('#donation-error'),panel=document.querySelector('#embedded-payment'),mount=document.querySelector('#checkout');let checkout;form.addEventListener('submit',async event=>{event.preventDefault();error.hidden=true;const button=form.querySelector('button');button.disabled=true;button.textContent='Preparing secure payment…';try{if(!form.dataset.stripeKey)throw new Error('Stripe publishable key is missing.');const response=await fetch(form.action,{method:'POST',body:new FormData(form),credentials:'same-origin'}),data=await response.json();if(!response.ok)throw new Error(data.error||'Unable to prepare Stripe Checkout.');if(checkout)checkout.destroy();mount.replaceChildren();const stripe=Stripe(form.dataset.stripeKey);checkout=await stripe.initEmbeddedCheckout({fetchClientSecret:async()=>data.client_secret});checkout.mount('#checkout');panel.hidden=false;panel.scrollIntoView({behavior:'smooth',block:'start'});}catch(exception){error.textContent=exception.message;error.hidden=false;}finally{button.disabled=false;button.textContent='Show secure payment form';}});})();
